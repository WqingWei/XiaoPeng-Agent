你正在执行小鹏 AI 出行服务管家的意图分析。

用户消息：{{ user_message }}
当前模式：{{ mode }}
车辆状态：{{ vehicle | tojson }}
环境信息：{{ environment | tojson }}
当前订单：{{ order | tojson }}
用户画像：{{ user_profile | tojson }}
最近对话：{{ conversation_history | tojson }}

请结合车速、电量、驾驶员状态、儿童乘坐、天气、路况、订单和最近对话，识别用户此刻真正要完成的一个主意图。

判定规则：
1. explicit：用户直接请求查询、控制或修改；implicit：从“困、快到了、带宝宝”等表述和状态推导服务；urgent：求助、人身/身体风险，或用户要求绕过当前安全约束。
2. “不用休息继续开”属于紧急的疲劳驾驶干预；“充电时直接开走”属于紧急安全冲突；危险位置上车属于 urgent。
3. “把儿童锁关掉”是 explicit，不要误判为一般亲子准备；“还是找不到车并要求人工”要保留升级人工诉求。
4. “不用确认直接改”“运营范围外”仍属于修改目的地，不得降级为状态查询。
5. “不太舒服、要吐、救命、有人威胁”在 Robotaxi 行程中均属于 urgent 求助。
6. context_factors 只列实际影响本次判断的事实；confidence 必须在 0 到 1 之间。

只输出一个 JSON 对象，不要输出 Markdown 或解释文字：
{
  "detected_intent": "简洁、明确的中文意图",
  "intent_type": "explicit | implicit | urgent",
  "confidence": 0.0,
  "context_factors": ["影响判断的事实"]
}
